image: node:alpine
include:
  project: 'ezapi_poc/infra_poc'
  file: 
    - 'reactuici.yaml'
    - 'deploy-reactui-demo-1.yaml'
stages:
  - build
  - deploy 
build:
  script:
    useTemplate:
      image: node:alpine
      extends: .build
deploy:  
  script:  
    useTemplate:
      image: alpine  
      extends: .deploy-demo1

