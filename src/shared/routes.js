const routes = Object.freeze({
  root: "/",
  signIn: "/signin",
  projects: "/projects",
  project: "/projects/:id",
});

export const generateRoute = (route, data) => {
  return "/";
};

export default routes;
